<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.hting.mapper.oracle.TbDistrictMapper1">


    <insert id="save">
        insert into TB_DISTRICT (ID, NAME, "level", "parent", CODE, BD_COORDINATOR, GPS_COORDINATOR) values (

            #{id},
            #{name},
            #{level},
            #{parent},
            #{code,jdbcType=VARCHAR},
            #{bd_coordinator,jdbcType=CLOB},
            #{gps_coordinator,jdbcType=CLOB}


        )

    </insert>
</mapper>